{% block form_row %}
    {% if form.vars.type is defined and form.vars.type == 'date' %}
        <div class="form--item form--input-focused">
    {% elseif form.vars.choices is defined %}
        <div class="form--item form--item-select">
    {% else %}
        <div class="form--item">
    {% endif %}
        {{- form_label(form) -}}
        {{- form_errors(form) -}}
        {{- form_widget(form) -}}
    </div>
{%- endblock form_row -%}

{% block text_widget %}
    {% set type = type|default('text') %}
    {{ block('form_widget_simple') }}
{% endblock %}

{% block file_widget %}
    {% set type = type|default('file') %}
    {{ block('form_widget_simple') }}
    <div class="form--item-file-previews">
        <img class="form--item-file-preview" src="{{ asset('uploads/raids/' ~ form.vars.data) }}" alt=""/>
    </div>
{% endblock %}

{% block form_errors %}
    {%- if errors|length > 0 -%}
    <ul class="form--error">
        {%- for error in errors -%}
            <li>{{ error.message }}</li>
        {%- endfor -%}
    </ul>
    {%- endif -%}
{% endblock form_errors %}

{%- block form_widget_simple -%}
    {%- set type = type|default('text') -%}
    <input class="form--input-{{ type }}" type="{{ type }}" {{ block('widget_attributes') }} {% if value is not empty %}value="{{ value }}" {% endif %}/>
{%- endblock form_widget_simple -%}